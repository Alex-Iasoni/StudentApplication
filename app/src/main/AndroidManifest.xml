<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.isen.iasoni.studentapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.RegisterActivity2"></activity>
        <activity android:name=".View.RegisterActivity1" />
        <activity android:name=".View.PassedEventsActivity" />
        <activity android:name=".View.CreateEventActivity" />
        <activity android:name=".View.NotifActivity" />
        <activity android:name=".View.EventsActivity" />
        <activity android:name=".View.LoginActivity" />
        <activity android:name=".View.RegisterActivity" />
        <activity android:name=".View.SwipeActivity" />
        <activity android:name=".View.FilterActivity" />
        <activity android:name=".View.IntroductionActivity" />
        <activity android:name=".View.ProfilActivity">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    </application>

</manifest>